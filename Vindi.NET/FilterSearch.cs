namespace Vindi.NET
{
    public enum FilterSearch
    {
        id,
        code,
        installments,
        period_id,
        subscription_id,
        customer_id,
        amount,
        status,
        payment_method_id,
        seen_at,
        due_at,
        billing_at,
        created_at,
        updated_at,
        name,
        email,
        registry_code,
        interval_count,
        interval,
        billing_cycles,
        billing_trigger_day,
        billing_trigger_type,
        invoice,
        unit,
        pricing_schema_id,
        schema_type,
        price,
        bill_id,
        paid_at,
        attempt_count,
        next_attempt,
        start_at,
        end_at,
        cycle,
        duration,
        plan_id,
        cancel_at,
        overdue_since,
        integration_id,
        integration_invoice_id,
        integration_reference,
        issued_at,
        integration_type,
        notification_type,
        notification_id,
        charge_id,
        batch_file_size,
        batch_file_name,
        batch_fingerprint,
        item_id,
        item_type,
        issue_type

    }
}
